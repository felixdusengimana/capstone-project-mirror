export interface IIndustry {
  id: number;
  name: string;
  code: string;
}

export interface ICountry {
  id: number;
  name: string;
  isoCode: string;
  countryCode: string;
  currency: string;
}

export interface IBank {
  id: number;
  name: string;
  code: string;
}
